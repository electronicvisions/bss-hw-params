#include <stddef.h>

namespace bss_hw_params {
namespace cube_extoll {

/**
 * FPGA bitfile compatibility.
 * Incremented on breaking changes within the FPGA design.
 */
static constexpr size_t bitfile_compatible_until = 0;

} // namespace cube_extoll
} // namespace bss_hw_params
